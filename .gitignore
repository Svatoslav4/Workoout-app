# Загальні файли та папки Node.js
/node_modules
/build
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Файли блокування залежностей (package.json і package-lock.json)
# Зазвичай пушиться тільки package.json, 
# але ти попросив не пушити package-lock.json і yarn.lock

yarn.lock 

# Файли конфігурації TypeScript, якщо ти не хочеш їх пушити
# (хоча tsconfig.json зазвичай пушиться для консистентності)

*.tsbuildinfo

# Інші файли, які можуть бути згенеровані
.DS_Store
.env